#include<stdio.h>
int main()
        {
            float c, f;
            printf("Enter celcius number: ");
            scanf("%f", &c);
            f = (c * 1.8) + 32;
            printf("Fahrenheit: %f", f);
        }
