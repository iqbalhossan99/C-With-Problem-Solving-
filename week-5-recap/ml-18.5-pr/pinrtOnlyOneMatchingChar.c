#include<stdio.h>
int main()
{
//    You are given a string S .Remove all the repeated character from the given string. You can’t change the order of
//the character in the time of printing. See the sample output for more clarification.
//
//Sample Input: dddaaabccdffgzzza
// Sample Output: dabcdfgza

    int n,i;
    scanf("%d", &n);
    char  arrChar[100];
    char filterArr[100];

  fgets(arrChar, sizeof(arrChar), stdin);

    for(i=0; i<n; i++)
    {
       if()

    }

    for(i=0; i<n; i++)
    {
        printf("%c ", arrChar[i]);

    }

    return 0;
}
